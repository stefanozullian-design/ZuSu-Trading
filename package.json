{
  "name": "zusu-trading",
  "private": true,
  "version": "0.1.0",
  "description": "ZuSu Trading \u2014 day-trading automation platform (DEMO / PAPER / LIVE)",
  "engines": {
    "node": ">=20.11"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @zusu/shared && npm run build -w @zusu/api && npm run build -w @zusu/web",
    "dev": "npm run dev -w @zusu/api & npm run dev -w @zusu/web",
    "dev:api": "npm run dev -w @zusu/api",
    "dev:web": "npm run dev -w @zusu/web",
    "typecheck": "npm run typecheck -w @zusu/shared && npm run typecheck -w @zusu/api && npm run typecheck -w @zusu/web && tsc -p e2e/tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings=0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run test -w @zusu/shared && npm run test -w @zusu/api && npm run test -w @zusu/web",
    "test:unit": "npm run test:unit -w @zusu/api",
    "test:integration": "npm run test:integration -w @zusu/api",
    "backfill:demo": "npm run backfill:demo -w @zusu/api",
    "db:generate": "npm run db:generate -w @zusu/api",
    "db:migrate": "npm run db:migrate -w @zusu/api",
    "db:deploy": "npm run db:deploy -w @zusu/api",
    "db:reset": "npm run db:reset -w @zusu/api",
    "seed": "npm run seed -w @zusu/api",
    "docs:openapi": "npm run docs:openapi -w @zusu/api",
    "test:e2e": "playwright test --config e2e/playwright.config.ts",
    "test:e2e:ui": "playwright test --config e2e/playwright.config.ts --ui"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@playwright/test": "^1.63.0",
    "eslint": "^9.13.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.11.0"
  },
  "type": "module"
}
